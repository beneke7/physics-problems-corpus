# Internal archive view

Open `/physics-problems-corpus/internal/` on GitHub Pages and enter the shared key.

This is only a casual UI gate. GitHub Pages is public, so the Markdown files,
figures, and manifest remain directly downloadable by anyone who knows the
paths. The key is not a licence or an access-control mechanism. Raw source PDFs
remain outside this repository.
