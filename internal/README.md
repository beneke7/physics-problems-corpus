# Internal archive view

Open `/physics-problems-corpus/internal/` on GitHub Pages and enter the shared key.

This is only a casual UI gate. GitHub Pages is public, so the Markdown files and
the manifest remain directly downloadable by anyone who knows the paths. The
key is not a licence or an access-control mechanism. Raw source PDFs and the
large figure tree are intentionally not copied into this repository.
