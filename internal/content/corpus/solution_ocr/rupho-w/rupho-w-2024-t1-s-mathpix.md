---
id: solution-ocr-rupho-w-2024-t1-s
source: rupho-w
kind: official_solution_document
language: ru
solution_type: official
source_pdf: cache/phoxiv/rupho-w/2024/T1_S.pdf
extraction_method: mathpix-pdf-api-md
mapped_problems: []
verification_status: pending
provenance_note: "Mathpix PDF API Markdown conversion; source transcription pending human verification."
---
Первое решение: Длина свисающей справа части верёвки равна $L _ { 2 } = L _ { 1 } + L \sin \alpha$, а полная длина верёвки составляет:

$$
l = 2 L _ { 1 } + L ( 1 + \sin \alpha ) .
$$

Воспользуемся законом сохранения механической энергии:

$$
\frac { m v ^ { 2 } } { 2 } = - \Delta W _ { p }
$$

где $m = \lambda l$ - масса верёвки, а $\Delta W _ { p }$ - изменение её потенциальной энергии в поле тяжести.
Изменение потенциальной энергии верёвки в поле тяжести равно изменению потенциальной энергии участка верёвки длиной $x$, координата $x _ { C }$ центра масс которого изменилась от значения $x _ { C _ { 1 } } = - x / 2$ до $x _ { C _ { 2 } } = x / 2$. Отсюда имеем:

$$
\Delta W _ { p } = - \lambda x \cdot g \cdot \left( x _ { C _ { 2 } } - x _ { C _ { 1 } } \right) = - \lambda g x ^ { 2 } .
$$

Подставляя $\Delta W _ { p }$ в закон сохранения механической энергии, получим:

$$
\frac { m v ^ { 2 } } { 2 } = \frac { \lambda l v ^ { 2 } } { 2 } = \lambda g x ^ { 2 } \Rightarrow v = x \sqrt { \frac { 2 g } { l } } ,
$$

откуда:

Ответ:

$$
v = x \sqrt { \frac { 2 g } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } } \text {. }
$$

Поскольку скорость $v ( x )$ и перемещение $x$ связаны прямой пропорциональностью:

$$
a = v \sqrt { \frac { 2 g } { l } } = \frac { 2 g x } { l } ,
$$

откуда:

Ответ:

$$
a = \frac { 2 g x } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } .
$$

Второе решение: Запишем уравнение движения каждого из участков верёвки:

$$
\lambda \left( L _ { 1 } + x \right) a = \lambda \left( L _ { 1 } + x \right) g - T _ { 1 } \quad \lambda L a = T _ { 1 } - T _ { 2 } + \lambda g L \sin \alpha \quad \lambda \left( L _ { 2 } - x \right) a = T _ { 2 } - \lambda \left( L _ { 2 } - x \right) g ,
$$

где $T _ { 1 }$ и $T _ { 2 }$ - силы натяжения верёвки вблизи блоков 1 и 2 соответственно.
Складывая уравнения, получим:

$$
\lambda \left( L _ { 1 } + L _ { 2 } + L \right) a = \lambda g \left( L _ { 1 } + L \sin \alpha - L _ { 2 } + 2 x \right) ,
$$

откуда

Ответ:

$$
a = \frac { 2 g x } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } .
$$

Домножая выражение для ускорения на скорость, получим:

$$
a v = \frac { d \left( v ^ { 2 } / 2 \right) } { d t } = \frac { 2 g x v } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } = \frac { g } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } \frac { d \left( x ^ { 2 } \right) } { d t }
$$

Интегрируя, получим:

$$
\frac { v ^ { 2 } } { 2 } = \frac { g x ^ { 2 } } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } ,
$$

откуда:


Ответ:

$$
v = x \sqrt { \frac { 2 g } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } }
$$

A2 ${ } ^ { 0.80 }$ Определите величину смещения левого конца верёвки $x _ { 1 }$ в тот момент времени, когда натяжение участка веревки, лежащего на наклонной плоскости, постоянно по длине этого участка. Ответ выразите через $L _ { 1 } , L$ и $\alpha$. При каких соотношениях $L _ { 1 } , L$ и $\alpha$ величина $x _ { 1 }$ меньше начальной длины правого участка верёвки?

Введём ось $y$, направленную вдоль наклонной плоскости от блока 1 к блоку 2. Запишем в проекции на ось $y$ второй закон Ньютона для участка верёвки длиной $d y$, лежащего на наклонной плоскости:

$$
d T + \lambda g \sin \alpha d y = \lambda a d y .
$$

Величина $d T = 0$ при $a \left( x _ { 1 } \right) = g \sin \alpha$. Отсюда:

$$
\frac { 2 g x _ { 1 } } { l } = g \sin \alpha \Rightarrow x _ { 1 } = \frac { l \sin \alpha } { 2 } ,
$$

откуда:

Ответ:

$$
x _ { 1 } = L _ { 1 } \sin \alpha + \frac { L \sin \alpha ( 1 + \sin \alpha ) } { 2 } .
$$

Сравним величину $x _ { 1 }$ с величиной $L _ { 2 }$ :

$$
x _ { 1 } = L _ { 1 } \sin \alpha + \frac { L \sin \alpha ( 1 + \sin \alpha ) } { 2 } \quad L _ { 2 } = L _ { 1 } + L \sin \alpha .
$$

Коэффициенты перед $L _ { 1 }$ и $L$ в выражении для $L _ { 2 }$ больше соответствующих коэффициентов в выражении для $x _ { 1 }$, поэтому:

Ответ: При любых соотношениях $L _ { 1 } , L$ и $\alpha$.

А3 ${ } ^ { 1.50 }$ Определите максимальную силу натяжения верёвки в момент времени, когда ее левый конец сместился на расстояние $x$. Ответ выразите через $\lambda$, $g$, $L _ { 1 } , L , \alpha$ и $x$.

На вертикальных участках верёвки наибольшая сила натяжения достигается в точках касания блоков, поскольку ускорение $a ( x )$ всегда меньше $g$. Обозначим за $T _ { 1 }$ и $T _ { 2 }$ силу натяжения верёвки в точках касания блоков 1 и 2 соответственно.
Из второго закона Ньютона для левого вертикального участка верёвки получим:

$$
\lambda \left( L _ { 1 } + x \right) g - T _ { 1 } = \lambda \left( L _ { 1 } + x \right) a \quad T _ { 2 } - \lambda \left( L _ { 2 } - x \right) g = \lambda \left( L _ { 2 } - x \right) a ,
$$

откуда:

$$
T _ { 1 } = \lambda \left( L _ { 1 } + x \right) ( g - a ) \quad T _ { 2 } = \lambda \left( L _ { 2 } - x \right) ( g + a ) .
$$

Проанализируем наклонный участок верёвки. Из решения пункта А2 следует:

$$
\frac { d T } { d y } = \lambda ( a - g \sin \alpha ) .
$$

При $x \leq x _ { 1 }$ величина $d T / d y$ не положительна, а при $x \geq x _ { 1 }$ - неотрицательна, откуда следует, что:

$$
T _ { \max } = \left\{ \begin{array} { l l l }
T _ { 1 } & \text { при } & x \geq x _ { 1 } \\
T _ { 2 } & \text { при } & x \leq x _ { 1 }
\end{array} \right.
$$

Подставляя выражения для $a , L _ { 2 }$ и $x _ { 1 }$, получим:

Ответ:

$$
T _ { \max } = \left\{ \begin{array} { l }
\frac { \lambda g \left( L _ { 1 } + x \right) \left( 2 L _ { 1 } + L ( 1 + \sin \alpha ) - 2 x \right) } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } \quad \text { при } \quad x \geq L _ { 1 } \sin \alpha + \frac { L \sin \alpha ( 1 + \sin \alpha ) } { 2 } \\
\frac { \lambda g \left( L _ { 1 } + L \sin \alpha - x \right) \left( 2 L _ { 1 } + L ( 1 + \sin \alpha ) + 2 x \right) } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } \quad \text { при } \quad x \leq L _ { 1 } \sin \alpha + \frac { L \sin \alpha ( 1 + \sin \alpha ) } { 2 }
\end{array} \right.
$$


Запишем для участка верёвки массой $d m = \lambda d l$ второй закон Ньютона:

$$
d m \vec { a } = d \vec { T } + d m \vec { g } + d \vec { N }
$$

При отрыве $d \vec { N } = 0$, поэтому:

$$
d m \vec { a } = d \vec { T } + d m \vec { g } .
$$

Пусть $\rho$ - радиус кривизны рассматриваемой поверхности, тогда $d l = \rho d \varphi$, где $d \varphi$ - угол поворота касательной к верёвке на рассматриваемом участке $d l$. Спроецируем уравнение движения на направление нормали, направленное к центру кривизны:

$$
d m a _ { n } = \lambda \rho d \varphi \cdot \frac { v ^ { 2 } } { \rho } = \lambda v ^ { 2 } d \varphi = T d \varphi + \lambda \rho d \varphi \cdot g _ { n }
$$

где $g _ { n }$ - проекция ускорения свободного падения на направление нормали.
Поскольку радиус блоков мал - условие отрыва следующее:

$$
T = \lambda v ^ { 2 } .
$$

Временно переобозначим величины $x _ { \max ( 1 ) }$ и $x _ { \max ( 2 ) }$ за $x _ { 1 }$ и $x _ { 2 }$ соответственно.
Уравнение для $x _ { 1 }$ следующее:

$$
\frac { \lambda g \left( L _ { 1 } + x _ { 1 } \right) \left( 2 L _ { 1 } + L ( 1 + \sin \alpha ) - 2 x _ { 1 } \right) } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } = \frac { 2 \lambda g x _ { 1 } ^ { 2 } } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } .
$$

откуда:

$$
4 x _ { 1 } ^ { 2 } - x _ { 1 } L ( 1 + \sin \alpha ) - L _ { 1 } \left( 2 L _ { 1 } + L ( 1 + \sin \alpha ) \right) = 0 .
$$

У данного квадратного уравнения один положительный корень, поэтому:

Ответ:

$$
x _ { \max ( 1 ) } = \frac { L ( 1 + \sin \alpha ) } { 8 } + \sqrt { \left( \frac { L ( 1 + \sin \alpha ) } { 8 } \right) ^ { 2 } + \frac { L _ { 1 } \left( 2 L _ { 1 } + L ( 1 + \sin \alpha ) \right) } { 4 } } .
$$

Уравнение для $x _ { 2 }$ следующее:

$$
\frac { \lambda g \left( L _ { 1 } + L \sin \alpha - x _ { 2 } \right) \left( 2 L _ { 1 } + L ( 1 + \sin \alpha ) + 2 x _ { 2 } \right) } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } = \frac { 2 \lambda g x _ { 2 } ^ { 2 } } { 2 L _ { 1 } + L ( 1 + \sin \alpha ) } ,
$$

откуда:

$$
4 x _ { 2 } ^ { 2 } + x _ { 2 } L ( 1 - \sin \alpha ) - \left( L _ { 1 } + L \sin \alpha \right) \left( 2 L _ { 1 } + L ( 1 + \sin \alpha ) \right) = 0 .
$$

У данного квадратного уравнения один положительный корень, поэтому:

Ответ:

$$
x _ { \max ( 2 ) } = - \frac { L ( 1 - \sin \alpha ) } { 8 } + \sqrt { \left( \frac { L ( 1 - \sin \alpha ) } { 8 } \right) ^ { 2 } + \frac { \left( L _ { 1 } + L \sin \alpha \right) \left( 2 L _ { 1 } + L ( 1 + \sin \alpha ) \right) } { 4 } } .
$$

А5 ${ } ^ { 1.20 }$ Рассчитайте скорость верёвки в момент её отрыва от одного из блоков, если наклонная плоскость образует с горизонтом углы $\alpha _ { 1 } = 30 ^ { \circ }$, $\alpha _ { 2 } = 45 ^ { \circ }$ и $\alpha _ { 3 } = 60 ^ { \circ }$.

Для каждого из углов отрыв достигается при $x$, равном меньшей из соответствующих данному углу величин $x _ { \max ( 1 ) }$ и $x _ { \max ( 2 ) }$. Рассчитаем их:

| $\alpha$, ${ } ^ { \circ }$ | $x _ { \text {max } ( 1 ) }$, M | $x _ { \text {max } ( 2 ) }$, M |
| :--- | :--- | :--- |
| 30 | 0.777 | 0.731 |
| 45 | 0.833 | 0.868 |
| 60 | 0.876 | 0.973 |

Таким образом, при угле $\alpha _ { 1 }$ верёвка впервые теряет контакт со вторым блоком, а при углах $\alpha _ { 2 }$ и $\alpha _ { 3 }$ - с первым.
Отсюда находим:

Ответ:

$$
v _ { 1 } = 2.068 \mathrm {~m} / \mathrm { c } \quad v _ { 2 } = 2.264 \mathrm {~m} / \mathrm { c } \quad v _ { 3 } = 2.314 \mathrm {~m} / \mathrm { c } \text {. }
$$


В $1 ^ { 0.80 }$ Определите величину смещения $x _ { 0 }$ левого конца верёвки, соответствующую положению равновесия системы. Ответ выразите через $m , \lambda , L _ { 1 } , L , \alpha , g$, $k$ и $F _ { 0 }$.

Запишем условия равновесия для трёх участков верёвки:

$$
T _ { 1 } = \lambda g \left( L _ { 1 } + x _ { 0 } \right) + m g \quad T _ { 2 } - T _ { 1 } = \lambda g L \sin \alpha \quad T _ { 2 } = \lambda g \left( L _ { 2 } - x _ { 0 } \right) + F _ { \text {упр } } ,
$$

откуда:

$$
F _ { \text {упр } } = \lambda g \left( L \sin \alpha + L _ { 1 } + x _ { 0 } + x _ { 0 } - L _ { 1 } - L \sin \alpha \right) + m g = m g + 2 \lambda g x _ { 0 } .
$$

Поскольку $F _ { \text {упр } } = F _ { 0 } + k x _ { 0 }$, получим:

Ответ:

$$
x _ { 0 } = \frac { m g - F _ { 0 } } { k - 2 \lambda g } .
$$

В2 ${ } ^ { 0.70 }$ Положение равновесия из предыдущего пункта существует, если жесткость пружины превышает некоторое минимальное значение $k _ { \text {min } }$. Определите $k _ { \text {min } }$. Ответ выразите через $m , \lambda , L _ { 1 } , L , \alpha , g , k$ и $F _ { 0 }$. Далее везде считайте, что $k > k _ { \text {min } }$.

Знаменатель полученного в пункте B1 должен быть положительным, поэтому имеем ограничение:

$$
k > 2 \lambda g .
$$

Величина $x _ { 0 }$ также ограничена длиной $L _ { 2 }$ правого вертикального участка верёвки, поэтому:

$$
x _ { 0 } = \frac { m g - F _ { 0 } } { k - 2 \lambda g } \leq L _ { 1 } + L \sin \alpha \Rightarrow k \geq 2 \lambda g + \frac { m g - F _ { 0 } } { L _ { 1 } + L \sin \alpha } .
$$

Последнее ограничение является более существенным, поэтому имеем:

Ответ:

$$
k _ { \min } = 2 \lambda g + \frac { m g - F _ { 0 } } { L _ { 1 } + L \sin \alpha }
$$

Вз ${ } ^ { 0.80 }$ Найдите изменение потенциальной энергии системы $\Delta W _ { p }$ при смещении левого конца верёвки на величину $x$. Ответ выразите через $m , \lambda , L _ { 1 } , L , \alpha$, $g , k , F _ { 0 }$ и $x$.

Для величин $\Delta W _ { p ( \text { г } ) }$ и $\Delta W _ { p ( \text { в } ) }$ имеем:

$$
\Delta W _ { p ( \mathrm { r } ) } = - m g x \quad \Delta W _ { p ( \mathrm { r } ) } = - \lambda g x ^ { 2 } .
$$

Изменение потенциальной энергии деформации пружины $\Delta W _ { p ( д е ф ) }$ равно площади под графиком $F _ { \text {упр } } ( \Delta l )$ и составляет:

$$
\Delta W _ { p ( \text { деф } ) } = F _ { 0 } x + \frac { k x ^ { 2 } } { 2 } .
$$

Окончательно:

Ответ:

$$
\Delta W _ { p } = \frac { ( k - 2 \lambda g ) x ^ { 2 } } { 2 } - \left( m g - F _ { 0 } \right) x .
$$

В4 ${ } ^ { 0.70 }$ Если принять потенциальную энергию $W _ { p }$ в положении равновесия равной нулю, то ее можно представить в виде

$$
W _ { p } = \frac { A ( x - B ) ^ { 2 } } { 2 } .
$$

Определите $A$ и $B$. Ответы выразите через $m , \lambda , L _ { 1 } , L , \alpha , g , k$ и $F _ { 0 }$.

Из выражения для $\Delta W _ { p }$ имеем:

$$
W _ { p } = \frac { ( k - 2 \lambda g ) x ^ { 2 } } { 2 } - \left( m g - F _ { 0 } \right) x + C = \frac { k - 2 \lambda g } { 2 } \left( x - \frac { m g - F _ { 0 } } { k - 2 \lambda g } \right) ^ { 2 } - \frac { \left( m g - F _ { 0 } \right) ^ { 2 } } { 2 ( k - 2 \lambda g ) } + C .
$$

Определяя потенциальную энергию в положении равновесия равной нулю:


$$
W _ { p } = \frac { k - 2 \lambda g } { 2 } \left( x - \frac { m g - F _ { 0 } } { k - 2 \lambda g } \right) ^ { 2 } .
$$

Таким образом:

Ответ:

$$
A = k - 2 \lambda g \quad B = \frac { m g - F _ { 0 } } { k - 2 \lambda g }
$$

В5 ${ } ^ { 0.50 }$ Определите максимальную скорость груза $v _ { \max }$ и его смещение от начального положения $x _ { \max }$, если систему отпустить без начальной скорости. Ответы выразите через $m , \lambda , L _ { 1 } , L , \alpha , g , k$ и $F _ { 0 }$.

Максимальная скорость груза достигается в точке с координатой $x _ { 0 }$ и определяется из закона сохранения механической энергии:

$$
\frac { \left( m + \lambda \left( 2 L _ { 1 } + L ( 1 + \sin \alpha ) \right) v _ { \max } ^ { 2 } \right. } { 2 } = \frac { A x _ { 0 } ^ { 2 } } { 2 } ,
$$

откуда:

Ответ:

$$
v _ { \max } = \frac { m g - F _ { 0 } } { k - 2 \lambda g } \sqrt { \frac { k - 2 \lambda g } { m + \lambda \left( 2 L _ { 1 } + L ( 1 + \sin \alpha ) \right. } } .
$$

Величина $x _ { \text {max } }$ достигается в момент остановки груза, т.е определяется из условия:

$$
\frac { A \left( x _ { \max } - x _ { 0 } \right) ^ { 2 } } { 2 } = \frac { A x _ { 0 } ^ { 2 } } { 2 } \Rightarrow x _ { \max } = 2 x _ { 0 } ,
$$

откуда:

Ответ:

$$
x _ { \max } = \frac { 2 \left( m g - F _ { 0 } \right) } { k - 2 \lambda g } .
$$

В6 ${ } ^ { 1.00 }$ Какую минимальную скорость $v _ { \min }$ должен иметь груз в начальном положении, чтобы правый конец нити достиг уровня блока 2 ? Ответ выразите через $m , \lambda , L _ { 1 } , L , \alpha , g , k$ и $F _ { 0 }$.

Груз должен остановиться в момент, когда опускается на величину $L _ { 2 }$. Из закона сохранения механической энергии:

$$
\frac { A \left( L _ { 2 } - x _ { 0 } \right) ^ { 2 } } { 2 } = \frac { A x _ { 0 } ^ { 2 } } { 2 } + \frac { \left( m + \lambda \left( 2 L _ { 1 } + L ( 1 + \sin \alpha ) \right) v _ { \min } ^ { 2 } \right. } { 2 } ,
$$

откуда:

Ответ:

$$
v _ { \min } = \sqrt { \frac { k - 2 \lambda g } { m + \lambda \left( 2 L _ { 1 } + L ( 1 + \sin \alpha ) \right. } \left( L _ { 1 } + L \sin \alpha \right) \left( L _ { 1 } + L \sin \alpha - \frac { 2 \left( m g - F _ { 0 } \right) } { k - 2 \lambda g } \right) }
$$
